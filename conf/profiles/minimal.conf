# Minimal profile — bare essentials only
PROFILE_DESC="Minimal environment — git, curl, vim only"
PROFILE_PACKAGES=(git curl vim)
PROFILE_INSTALL_DOCKER=false
PROFILE_HARDEN=false
PROFILE_SETUP_USER=""
