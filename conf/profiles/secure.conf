# Secure profile — full environment + SSH hardening + firewall + deploy user
PROFILE_DESC="Secure server — full environment, SSH hardening, firewall, deploy user"
PROFILE_PACKAGES=(git curl wget vim htop tmux unzip tree net-tools ca-certificates build-essential ufw iproute2 dnsutils nmap python3)
PROFILE_INSTALL_DOCKER=true
PROFILE_HARDEN=true
PROFILE_SETUP_USER="deploy"
