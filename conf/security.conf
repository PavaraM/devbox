# Security configuration for DevBox
# ====================================================

# SSH hardening
SSH_PORT=22

# Firewall ports to open (space-separated)
FIREWALL_OPEN_PORTS="22 80 443"

# Deploy user settings
DEPLOY_USERNAME="deploy"
DEPLOY_SUDO_NOPASSWD=true
DEPLOY_GROUPS="docker,sudo"
