# Tool versions for mise (version manager)
# Format: tool=version  (e.g. nodejs=20.0.0, golang=1.22.5)
# Use "*" or "latest" for the latest available version.
# Tools listed here are installed via `mise install` when running with --with-mise.

# nodejs=20.0.0
# golang=1.22.5
# python=3.12.0
# ruby=3.3.0
